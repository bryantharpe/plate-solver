# Rust build artifacts
target/
**/target/
# cargo-fuzz
rust/canary/crates/canary-parse/fuzz/corpus/
rust/canary/crates/canary-parse/fuzz/artifacts/
# coverage / SBOM outputs
*.profraw
lcov.info
# editor/OS
.DS_Store
# tool outputs (regenerated by CI, never committed)
mutants.out/
mutants.out.old/
**/mutants.out/
**/mutants.out.old/
# canary SBOMs only — release SBOMs elsewhere in the tree stay committable
rust/canary/**/*.cdx.json
