# plate-solver: implement the tetra3/cedar lost-in-space plate solver in Rust (feat-01 … feat-06), spec-by-spec, via a Qwen coder + Opus judge

_Authored 2026-06-10. This file is the ONLY durable state for this loop. Every iteration re-reads it from disk; it must be self-sufficient for a cold restart._

## Purpose

**What:** Implement, as a single Cargo workspace, the six already-specified OpenSpec features `feat-01`…`feat-06` — `ps-core` (math), `ps-detect` (star detection), `ps-db` (pattern database read-side), `ps-dbgen` (offline DB generation), `ps-solve` (plate solver), `ps-grpc` (PlateSolver gRPC service) — the full lost-in-space pipeline (star detection → pattern-DB lookup → attitude recovery) exposed over gRPC, with **numerical parity to the Python/Rust reference** within each spec's stated tolerances.

**Why:** The OpenSpec docs in `openspec/changes/feat-0{1..6}/` are complete and `--strict`-validated; the reference is Python (tetra3, cedar-solve) plus a partial Rust detector (cedar-detect). The product needs the whole pipeline in Rust. The specs' parity scenarios are the contract. To conserve frontier tokens, **mechanical Rust coding is delegated to a local Qwen model** (`ps-coder`); **architecture, task design, and every review/judgment stay on Opus** (`ps-judge` + this orchestrator). Slower wall-clock is accepted in exchange for cheap grunt tokens. feat-07 (mobile-runtime) is explicitly OUT of this loop (user decision: stop at gRPC); the memory-mapped DB path still lands inside `ps-db`.

**Definition of done (observable):** every task checkbox below is `[x]`; `cargo build --workspace` exits 0; `cargo test --workspace` exits 0 with **every feature's parity test included and green within its stated tolerance**; `ps-grpc` builds and its ExtractCentroids-interop + end-to-end `SolveFromImage` parity tests pass; and `openspec/IMPLEMENTATION-STATUS.md` exists summarizing the six features and their parity outcomes. The loop halts when all boxes are `[x]` and the finalize task (F1) confirms a clean `cargo test --workspace`.

## Loop Protocol

This loop **delegates coding to a subagent**. The orchestrator (you, on Opus) never writes feature Rust directly; it designs, delegates, judges, gates, and commits. Per iteration:

1. **Re-read this file from disk** (only durable state): Purpose/DoD, all Guardrails, the task list, and the Decisions/Blocked logs.
2. **Select** the first task whose checkbox is unchecked AND whose `deps:` are all `[x]`.
3. **Orchestrate (Opus):** read the cited spec (`openspec/changes/feat-NN/specs/*/spec.md` + `design.md`) and the cited reference doc (`reference-solutions/docs/NN-*.md`) with targeted Read/Grep — or one `Explore` subagent for heavy reading. Make every architectural decision yourself (or via `ps-judge` Job B). Write a precise **implementation brief**: exact crate/files, function signatures, the load-bearing conventions, the fixtures to use, and the objective acceptance check.
4. **Delegate coding → `ps-coder`** (Qwen): hand it the brief. It writes/edits Rust, runs the cargo gate, and returns a structured `RESULT/FILES/GATE/OUTPUT/NOTES` report. (Trivial one-line edits MAY be done inline; default to delegating.)
5. **Judge → `ps-judge`** (Opus, Job A): give it the changed files + the task AC. If `VERDICT: FAIL`, return the issues to `ps-coder` and re-judge (max **3** coder↔judge rounds). Still failing → BLOCK (step 7).
6. **Gate & land (Opus):** independently run the task's integrity gate (`cargo test -p <crate>`, build, parity) — never trust a pasted summary, never weaken the gate. On green, mark the box `[x]`, append a one-line Run Log entry, and commit the work **and** this plan together with the task's exact commit message (explicit in-scope paths). Do **not** push.
7. **Stop/continue:** all boxes `[x]` → write `openspec/IMPLEMENTATION-STATUS.md` and exit. Selected task can't reach its AC → mark it `BLOCKED`, append to the Blocked Log (reason + recommended fix), continue to the next unblocked task. Otherwise loop to step 1.

## Guardrails (apply every iteration)

- **Git:** work on branch `feat/rust-implementation` (create it from the current branch `docs/openspec-plate-solver` before the first commit if it does not exist — the specs live on that branch). Commit after every task, work + this plan together, with the task's exact message, staging **explicit in-scope paths only** (the workspace crates, `tools/parity/`, `openspec/IMPLEMENTATION-STATUS.md`, root `README.md` in F1, `plan.md`). **Never `git add -A`** (avoid sweeping stray files). **Never commit to `main` or `docs/openspec-plate-solver`. Never force-push. Do not push** (local review only).
- **Integrity gates:** before each coding task's commit, run `cargo build -p <crate>` and `cargo test -p <crate>` (exit 0), including the task's named parity test green within tolerance; `ps-grpc` tasks also require `cargo build -p ps-grpc` (needs `protoc`). F1 runs `cargo test --workspace` + `cargo build --workspace`. **Never weaken a gate:** never `#[ignore]` or delete a parity test, never loosen a stated tolerance, never stub/short-circuit a check or delete an assertion to pass — fix the code. `ps-judge` independently re-runs the gate and inspects for faked/loosened checks; a false PASS is worse than a FAIL.
- **Delegation / model policy:** mechanical Rust implementation → **`ps-coder`** (`.claude/agents/ps-coder.md`, model `qwen3.6-27b`, local via LiteLLM at `localhost:4000`). Architecture, briefs, parity-fixture design, and **every** review → **Opus** (this loop + **`ps-judge`**, model `claude-opus-4-8`). **Judge every task** (Job A) before its commit. The LiteLLM router accepts only `qwen3.6-27b`, `claude-opus-4-8`, `claude-sonnet-4-6` (no `haiku`); any subagent must pin one of these. If the `ps-coder`/`ps-judge` agent types are not available, **BLOCK** with "restart Claude Code so `.claude/agents/` register" (they register only at session start). If Qwen tool-use proves unreliable mid-run, fall back to `claude-sonnet-4-6` for coding and record it in the Decisions Log — never silently move grunt work onto Opus without logging.
- **Data/scope discipline:** `reference-solutions/` and `openspec/` are **READ-ONLY** (cite, never modify — the specs are the contract; never run `openspec archive`). Implement **only** the cedar runtime path + offline generation in scope; tetra3's simpler detector (doc 03) and per-anchor DB enumeration (doc 05 §5.1), partial-sky DBs, and feat-07 mobile packaging are **non-goals** — do not build them. Write only Rust under the workspace crates, parity tooling under `tools/parity/`, fixtures under each crate's `tests/`, `openspec/IMPLEMENTATION-STATUS.md`, and (F1 only) root `README.md`. **Golden parity values are captured from the reference and committed as fixtures — never hand-fabricate a "golden" number** to make a test pass.
- **Cost controls:** prefer the distilled `reference-solutions/docs/NN` over raw source; delegate any raw-source reading to a single `Explore` subagent and keep only the distilled result. Keep `ps-coder` briefs tight and self-contained (Qwen has the cheap tokens; Opus does the thinking, not the typing). Bound coder↔judge to **3 rounds** → BLOCK. Network is allowed only for Phase-0 reference-env setup and `cargo`/`pip` downloads; **no unbounded retries** (max 3 attempts on any install/gate → BLOCK).
- **Don't-stall:** a task that can't reach its AC → mark `BLOCKED`, append to the Blocked Log (reason + recommended fix), move to the next unblocked task. The loop never spins on one task.
- **Context hygiene:** this file is the only durable state. One task per iteration; commit work + this plan together before ending an iteration so progress never lives only in context. Re-read this file on restart. Raw spec/reference text stays in subagent contexts (Explore/ps-coder/ps-judge) — only distilled briefs and verdicts return to the main loop. Record every autonomous decision in the Decisions Log and proceed on the most defensible default.

## Tasks

### Phase 0 — Readiness & scaffolding

- [ ] S1 — Branch, scaffolding & subagent readiness — deps: none — Create branch `feat/rust-implementation` off `docs/openspec-plate-solver` if absent; ensure `.gitignore` covers `/target` and `tools/parity/.venv`; verify `cargo`/`rustc`/`protoc` exist; verify the `ps-coder`/`ps-judge` agent types are registered by running a one-shot `ps-coder` smoke task (read a file, write `/tmp/ps_coder_smoke.txt`, run a bash command, return `RESULT: DONE`); commit the in-tree scaffolding (`.claude/agents/ps-coder.md`, `.claude/agents/ps-judge.md`, `plan.md`, `plan-openspec-docs.md`, `tools/setup-toolchain.sh`, `.gitignore`). — AC: on branch `feat/rust-implementation`; `cargo --version` and `protoc --version` exit 0; `ps-coder` smoke returns `RESULT: DONE` with `GATE … -> PASS`; `git log -1` shows the scaffolding commit. **BLOCK (do not fabricate)** if a tool is missing (→ "user installs Rust/protoc") or an agent type is unavailable (→ "restart Claude Code"). — commit: "chore: scaffold rust implementation loop (agents, plan, gitignore)"
- [ ] S2 — Cargo workspace root — deps: S1 — Create the workspace root `Cargo.toml` (`[workspace]`, `resolver = "2"`, empty `members`, and `[workspace.dependencies]` pinning the shared deps from `openspec/project.md` + feat designs: `nalgebra`, a KD-tree crate (e.g. `kiddo`), `memmap2`, `image` (0.25), `half`, `byteorder`, `clap`, and `tonic`/`prost`/`tonic-web`/`prost-types` at the versions in `feat-06-grpc-service/design.md`), plus `rust-toolchain.toml`. — AC: `cargo metadata --no-deps` exits 0; root `Cargo.toml` contains `[workspace]`; tonic/prost versions match feat-06 design.md. — commit: "build: initialize cargo workspace root"
- [ ] S3 — Reference parity environment — deps: S1 — Stand up the offline parity-capture env: create `tools/parity/.venv`, install the reference deps (`numpy`, `scipy`, `scikit-image`, `astropy`, plus whatever `reference-solutions/cedar-solve` requires per its `setup.py`/`requirements`), and prove the reference runs via `tools/parity/check_env.py` (imports cedar-solve `tetra3` and runs one tiny solve against `reference-solutions/cedar-solve/tetra3/data/default_database.npz`); add `tools/parity/README.md` documenting how to (re)capture fixtures. — AC: `tools/parity/.venv/bin/python tools/parity/check_env.py` exits 0 and prints a recovered RA/Dec; README present. **BLOCK** with the precise failing dependency if the reference cannot be made to import after ≤3 install attempts. — commit: "test: set up reference parity-capture environment"

### Phase A — feat-01 `ps-core` (math-core) · ref doc 02

- [ ] MC1 — Crate + parity harness — deps: S2, S3 — Create the `ps-core` lib crate (add to workspace), add `nalgebra`; create `ps-core/tests/fixtures/` and `tools/parity/capture_core.py` (emits golden JSON from the reference math); document the f64-compute / f32-store and `(y,x)`/`(0.5,0.5)` conventions in crate docs. — AC: `cargo test -p ps-core` exits 0 (scaffold ok); `capture_core.py` writes at least one `ps-core/tests/fixtures/*.json`. — commit: "feat(ps-core): scaffold math-core crate and parity fixtures"
- [ ] MC2 — Celestial vectors & angular distance — deps: MC1 — Implement RA/Dec ↔ unit-vector conversions and `angle_from_distance`/`distance_from_angle` using `2·asin(d/2)` (never `arccos`). — AC: `cargo test -p ps-core` green incl. round-trips (<1e-12) and parity vs captured reference (<1e-9). — commit: "feat(ps-core): celestial vector & angular-distance conversions"
- [ ] MC3 — Pinhole projection — deps: MC2 — pixels→camera vectors and camera→pixels with `scale_factor`, in-frame `keep` (drop `i ≤ 0`), `(y,x)`, boresight `(1,0,0)`. — AC: `cargo test -p ps-core` green incl. projection round-trip <1e-9 and reference parity. — commit: "feat(ps-core): pinhole projection (pixels↔vectors)"
- [ ] MC4 — Radial distortion — deps: MC3 — closed-form `undistort_centroids` and Newton–Raphson `distort_centroids` (tol 1e-6, maxiter 30), `k' = k·(2/width)²`. — AC: `cargo test -p ps-core` green incl. distort∘undistort round-trip < tol and reference parity. — commit: "feat(ps-core): single-parameter radial distortion"
- [ ] MC5 — Attitude (Wahba/SVD) — deps: MC2 — `find_rotation_matrix` (`H = imgᵀ·cat`, `R = U·Vᵀ`), `det(R) < 0` **reject** (no sign-flip), RA/Dec/Roll extraction from `R`. — AC: `cargo test -p ps-core` green: recovers a known `R` <1e-9, reflection rejected, extracted angles match reference. — commit: "feat(ps-core): Wahba/SVD attitude and RA/Dec/Roll extraction"
- [ ] MC6 — Edge-ratio key, hashing & probing — deps: MC2 — 6 edges → sort → normalize by largest → 5 ratios → quantize by `pattern_bins = round(1/(4·pattern_max_error))`; 64-bit `key_hash = Σ key[m]·bins^m`; table index (quadratic `·_MAGIC_RAND mod`, linear `mod`); open-addressing probe; 16-bit key pre-filter; centroid-distance ordering. — AC: `cargo test -p ps-core` green: **identical integer keys/hashes** vs captured reference; asserts `pattern_bins(0.001)==250` and `_MAGIC_RAND==2654435761`. — commit: "feat(ps-core): edge-ratio pattern key, hashing, open-addressing"
- [ ] MC7 — FOV, false-alarm & residuals — deps: MC3, MC5 — FOV estimation + fine FOV/distortion least-squares; binomial false-alarm (`prob_single = Nc·match_radius²`, `binom.cdf(n−(m−2), n, 1−prob_single)`, `/num_patterns` Bonferroni); residual stats RMSE/P90E/MAXE (arcsec). — AC: `cargo test -p ps-core` green: false-alarm probability matches reference <1e-6 rel; residual stats correct on a known set (P90E ≤ MAXE). — commit: "feat(ps-core): FOV refinement, binomial false-alarm, residuals"

### Phase B — feat-02 `ps-detect` (star detection) · ref doc 04

- [ ] SD1 — Crate, I/O types & golden centroids — deps: MC2 — Create `ps-detect` (dep `ps-core`, `image` 0.25); add a row-major 8-bit `GrayImage` wrapper + `StarDescription` type; define the brightest-first external `(x,y)` / internal `(y,x)` contract with `(0.5,0.5)` pixel center; capture golden centroids from the reference cedar-detect (build `reference-solutions/cedar-detect` or use its binding) on `cedar-detect/test_data` at `sigma=8` into `ps-detect/tests/fixtures/`. — AC: `cargo test -p ps-detect` exits 0 (scaffold); golden centroid fixtures exist for ≥1 test_data image at sigma=8. — commit: "feat(ps-detect): scaffold detection crate, I/O types, golden fixtures"
- [ ] SD2 — Noise & background estimation — deps: SD1 — 3 de-starred midline cuts (width `min(50,width/4)`), darkest-cut stddev, `noise_floor = 0.2`; histogram utils (`stats_for_histogram`, `estimate_dark_level`, `remove_stars_from_histogram`); region background estimate. — AC: `cargo test -p ps-detect` green: noise/background estimate matches reference within tolerance on a fixture. — commit: "feat(ps-detect): noise and background estimation"
- [ ] SD3 — Binning cascade & row normalization — deps: SD2 — 2×2 box filter `(p1+p2+p3+p4)/4`, {1,2,4,8} cascade with `higher_res_image`, optional per-row dark-level normalization (bias 2.0), pluggable `set_binner` hook. — AC: `cargo test -p ps-detect` green: binned image + cascade scale selection correct on a fixture. — commit: "feat(ps-detect): binning cascade and row normalization"
- [ ] SD4 — 1-D row gate & hot-pixel rejection — deps: SD3 — `row_min` cache-line coarse threshold; `gate_star_1d` (7-pixel integer gate, `sigma_noise_2 = max(round(2·σ·noise),2)`, `sigma_noise_3 = max(round(3·σ·noise),3)`, significance/monotonic/flat-top tests, skip 3 edge columns); `classify_pixel` Dark/Hot/Bright + `all_bright_are_hot` on the full-res block. — AC: `cargo test -p ps-detect` green: 1-D candidate set + hot-pixel drops match reference on a fixture. — commit: "feat(ps-detect): 1-D row gate and hot-pixel rejection"
- [ ] SD5 — Blob formation & 2-D gate — deps: SD4 — `form_blobs_from_candidates` (vertical ±3 x merge, union-find with recipient forwarding); `gate_star_2d` (core/neighbors/margin/perimeter boxes, all 7 tests, perimeter-noise inflation). — AC: `cargo test -p ps-detect` green: blob set + 2-D pass/fail decisions match reference on a fixture. — commit: "feat(ps-detect): blob formation and 2-D gate"
- [ ] SD6 — Centroid, brightness & end-to-end — deps: SD5 — sub-pixel centroid (axis projections + quadratic interp `p = 0.5·(a−c)/(a−2b+c)`, degenerate cases), perimeter-subtracted brightness + `num_saturated` + `peak_value`, brightness-descending sort; wire `get_stars_from_image` end-to-end (binning, hot pixels, `return_binned`, input-coordinate scaling). — AC: `cargo test -p ps-detect` green: **PARITY** — centroids on `cedar-detect/test_data` at sigma=8 match the reference within **±0.1 px** with identical brightness ranking for common stars. — commit: "feat(ps-detect): sub-pixel centroid, brightness, end-to-end pipeline"

### Phase C — feat-03 `ps-db` (pattern database, read-side) · ref doc 05 §6–7, doc 02 §6.2–6.4

- [ ] PD1 — Crate + on-disk layout — deps: MC6 — Create `ps-db` (dep `ps-core`, KD-tree crate, `memmap2`); define the `Database` struct + properties record; specify the native little-endian on-disk layout (typed arrays + versioned properties header) in design comments. — AC: `cargo test -p ps-db` exits 0 (scaffold); layout documented in code. — commit: "feat(ps-db): scaffold database crate and on-disk layout"
- [ ] PD2 — npz importer + native loader — deps: PD1 — `.npz → native` importer (parity bridge) reading `reference-solutions/cedar-solve/tetra3/data/default_database.npz`; loader for all arrays (`star_table` f32, catalog u8/u16/u32, `largest_edge` f16, `key_hashes` u16, `star_catalog_IDs`) + legacy-fallback property resolution. — AC: `cargo test -p ps-db` green: imports the reference `.npz` and reads back array shapes + properties matching it (including the documented legacy fallbacks). — commit: "feat(ps-db): npz importer and native-format loader"
- [ ] PD3 — Star KD-tree & nearby query — deps: PD2 — build + cache the star KD-tree over unit vectors at load; `nearby_stars(vector, radius)` via chord radius `2·sin(r/2)`, brightest-first. — AC: `cargo test -p ps-db` green: `nearby_stars` returns the brightest-first set matching a reference query on the imported DB. — commit: "feat(ps-db): star KD-tree and nearby-star query"
- [ ] PD4 — Hash lookup & pre-filters — deps: PD2 — key → `key_hash` → table index (quadratic/linear, via `ps-core`) + probe-chain gather to first empty slot; 16-bit key pre-filter; largest-edge/FOV pre-filter + edge-ratio band test. — AC: `cargo test -p ps-db` green: **PARITY** — a known key lookup returns the identical candidate slot set as the imported reference DB. — commit: "feat(ps-db): pattern hash lookup and pre-filters"
- [ ] PD5 — Memory-mapped load path — deps: PD4 — `memmap2` linear-probe loading path (narrow-FOV / too-big-for-RAM), zero-copy typed views. — AC: `cargo test -p ps-db` green: mmap path yields identical lookups + `nearby_stars` results to the in-RAM path on the reference DB. — commit: "feat(ps-db): memory-mapped linear-probe load path"

### Phase D — feat-04 `ps-dbgen` (offline DB generation) · ref doc 05

- [ ] DG1 — CLI + catalog parsers — deps: MC6, PD2 — Create `ps-dbgen` (CLI binary; dep `ps-core`, `ps-db`, KD-tree, `clap`); BSC5 binary parser (header sanity, equinox sign rule, mag/RA/Dec/PM) + HIP/TYC pipe-delimited parser (units, ID handling, empty-field skip), tested against committed byte-fixtures (no full catalog ships in-repo). — AC: `cargo test -p ps-dbgen` green: parsers decode committed BSC5/HIP/TYC byte-fixtures into the expected star records. — commit: "feat(ps-dbgen): CLI scaffold and BSC5/HIP/TYC catalog parsers"
- [ ] DG2 — Proper motion & cleanup — deps: DG1 — PM propagation to epoch (recover RA rate by dividing `pmRA = μ_α·cosδ` by `cosδ`) with `cosδ ≤ 0.05` pole guard; drop RA==0&Dec==0; sort by magnitude; cedar auto limiting-magnitude (`num_fovs · verification_stars_per_fov · 0.7`). — AC: `cargo test -p ps-dbgen` green: PM + cleanup + auto-magnitude on a fixture match the reference rule. — commit: "feat(ps-dbgen): proper-motion propagation and catalog cleanup"
- [ ] DG3 — Vectors, multiscale FOV & thinning — deps: DG2, PD3 — unit vectors + KD-tree; multiscale FOV ladder (`multiscale_step = 1.5`, `2^linspace` divisions, dedup); density thinning `0.6·fov/√n` cluster-buster (greedy brightest-first, KD radius query). — AC: `cargo test -p ps-dbgen` green: FOV ladder + thinned star set are deterministic and match the reference rule on a fixture. — commit: "feat(ps-dbgen): unit vectors, multiscale FOV ladder, density thinning"
- [ ] DG4 — Lattice-field pattern enumeration — deps: DG3 — Fibonacci lattice fields (`num_fields_for_sky · lattice_field_oversampling=100`), gather stars within `pattern_fov/2`, up to `patterns_per_lattice_field=50` per field via `breadth_first_combinations(field_stars, 4)`, global dedup on the canonical sorted index tuple. — AC: `cargo test -p ps-dbgen` green: deterministic pattern set on a small sky region; dedup verified. — commit: "feat(ps-dbgen): Fibonacci lattice-field pattern enumeration"
- [ ] DG5 — Hashing/insert & serialization — deps: DG4, PD4 — edge-ratio key → hash → table index; open-address insert presorted by centroid distance; fill `largest_edge` (`L·1000` mrad, f16) + `key_hashes` (`key_hash & 0xFFFF`, u16); table size `next_prime(2N)` (quad) / `next_prime(3N)` (linear); serialize via the `ps-db` format writer. — AC: `cargo test -p ps-dbgen` green: generated DB round-trips cleanly through the `ps-db` loader; two runs are **byte-identical** (determinism). — commit: "feat(ps-dbgen): pattern hashing/insert and serialization"
- [ ] DG6 — End-to-end build & validation — deps: DG5 — Wire the CLI to build a small whole-sky DB from an available catalog (fetch the small public BSC5 if network is permitted, else a committed sample) and validate: `ps-db` loads it and answers a sample lookup; determinism holds. Full count-parity vs `default_database.npz` is **best-effort** — the reference's source catalog (Hipparcos/Tycho) is not in-repo, so if unmatched, assert structural validity + determinism and **log count-parity as deferred**. — AC: `cargo run -p ps-dbgen -- <args>` produces a DB that `ps-db` loads and looks up a known pattern; byte-identical rebuild holds; parity-vs-reference recorded (matched, or deferred-with-reason in the Decisions/Blocked log). — commit: "feat(ps-dbgen): end-to-end database build and validation"

### Phase E — feat-05 `ps-solve` (plate solver) · ref doc 06

- [ ] SV1 — Crate, types & API — deps: MC7, PD5, SD6 — Create `ps-solve` (dep `ps-core`, `ps-db`, `ps-detect`); define `Solution`/`SolveStatus`; `solve_from_centroids` + `solve_from_image` signatures with cedar default params (`match_radius=0.01`, `match_threshold=1e-5`, `match_max_error=0.002`, `solve_timeout=5000`, `distortion=0`). — AC: `cargo test -p ps-solve` exits 0 (scaffold); signatures compile; absent `fov_estimate` defaults to the DB FOV-range midpoint. — commit: "feat(ps-solve): scaffold solver crate, Solution/SolveStatus types"
- [ ] SV2 — Preparation & cluster-busting — deps: SV1 — set `fov_initial`; `match_threshold /= num_patterns` (Bonferroni); limit to brightest `verification_stars_per_fov`; undistort if scalar `k` (else defer); centroid cluster-bust via the density separation rule (KD-tree); precompute centroid vectors; `<4` → `TOO_FEW`. — AC: `cargo test -p ps-solve` green: prep produces the cluster-busted centroid set + `TOO_FEW` guard matching the reference on a fixture. — commit: "feat(ps-solve): centroid preparation and cluster-busting"
- [ ] SV3 — Candidate generation — deps: SV2, PD4 — breadth-first 4-star image-pattern iteration with `solve_timeout` (→`TIMEOUT`) and cancel flag (→`CANCELLED`) checked each iteration; image-pattern key + tolerance band (`p_max_err = max(match_max_error, DB pattern_max_error)`); nearest-first candidate-key enumeration (squared key distance); wire `ps-db` lookup + pre-filters + edge-ratio band → valid patterns. — AC: `cargo test -p ps-solve` green: candidate keys ordered nearest-first; `TIMEOUT`/`CANCELLED` paths reachable. — commit: "feat(ps-solve): breadth-first candidate generation and key search"
- [ ] SV4 — Verification & binomial accept — deps: SV3, MC5 — coarse FOV from largest-edge ratio; pair 4↔4 by centroid-distance order; SVD attitude `R`, reject `det(R)<0`; gather diagonal-FOV catalog stars, derotate+project (keep in-frame), trim to `2·num_centroids`, unique 1:1 match within `match_radius·width`; binomial accept (`prob_mismatch < match_threshold`); first acceptable wins. — AC: `cargo test -p ps-solve` green: verification accepts the correct pattern on a fixture; `det(R)<0` rejected; mismatch below threshold accepted. — commit: "feat(ps-solve): verification, projection matching, binomial accept"
- [ ] SV5 — Refinement, outputs & status — deps: SV4 — re-fit attitude over all matches; extract RA/Dec/Roll; refine FOV (+ distortion `k` least squares when not a fixed scalar); residuals RMSE/P90E/MAXE; assemble `Solution` + optional outputs (matched centroids/stars/IDs, catalog, rotation, target pixel/sky); status set `MATCH_FOUND | NO_MATCH | TIMEOUT | CANCELLED | TOO_FEW`. — AC: `cargo test -p ps-solve` green: full `Solution` fields populated; all five status codes reachable. — commit: "feat(ps-solve): refinement, outputs, and status codes"
- [ ] SV6 — Reference-parity solve — deps: SV5 — capture cedar's recorded solution (RA/Dec/Roll/FOV + matched catalog-ID set) for a `medium_fov` reference image via the Python reference + `default_database.npz` (using the `ps-db`-imported DB at runtime); assert `solve_from_image`/`solve_from_centroids` matches within a few arcsec with the identical matched catalog-ID set. — AC: `cargo test -p ps-solve` green: **PARITY** — RA/Dec within a few arcsec of cedar, matched catalog-ID set identical, status `MATCH_FOUND`. — commit: "feat(ps-solve): end-to-end reference-parity solve test"

### Phase F — feat-06 `ps-grpc` (PlateSolver gRPC service) · ref doc 07

- [ ] RPC1 — Proto & tonic build — deps: SV5, SD6 — Create `ps-grpc` (dep `ps-detect`, `ps-solve`, `ps-db`, `tonic`/`prost`/`tonic-web`/`prost-types`); author `plate_solver.proto` from the `feat-06-grpc-service/design.md` sketch (reuse `cedar_detect.proto` message shapes verbatim for `Image`/`ImageCoord`/`StarCentroid`); wire `build.rs` (`tonic-build`, requires `protoc`). — AC: `cargo build -p ps-grpc` exits 0 (the proto compiles). — commit: "feat(ps-grpc): plate_solver proto and tonic build"
- [ ] RPC2 — ExtractCentroids RPC — deps: RPC1 — `ExtractCentroids` over `ps-detect` (inline `image_data` path; fill `noise_estimate`/`hot_pixel_count`/`peak_star_pixel`/`algorithm_time`, brightest-first centroids); shared-memory path mapping `shmem_name` read-only with **INTERNAL-on-failure** → inline fallback; `reopen_shmem` handling. — AC: `cargo test -p ps-grpc` green: in-process `ExtractCentroids` returns brightest-first centroids matching `ps-detect`; a shared-memory failure yields gRPC `INTERNAL`. — commit: "feat(ps-grpc): ExtractCentroids RPC with shared-memory fast path"
- [ ] RPC3 — Solve RPCs & coordinate swap — deps: RPC2, SV6 — `SolveFromCentroids` (boundary `(x,y) → (y,x)` swap, forward `SolveParams`) and `SolveFromImage` (extract then solve, record `t_extract`/`t_solve`); map `ps-solve` outputs (status enum, optional matched stars) → `Solution`. — AC: `cargo test -p ps-grpc` green: `SolveFromImage` on a reference image returns `Solution{status=MATCH_FOUND}` matching `ps-solve`; a dedicated `(x,y)↔(y,x)` swap test passes. — commit: "feat(ps-grpc): SolveFromCentroids/SolveFromImage and coordinate swap"
- [ ] RPC4 — GetInfo, transport & interop — deps: RPC3 — `GetInfo` (version + DB properties + FOV range); serve over TCP `127.0.0.1:50051` (configurable) + gRPC-Web over HTTP/1; map errors/timeouts → gRPC status codes; an `ExtractCentroids` interop test driven by a `cedar_detect.proto`-shaped client. — AC: `cargo test -p ps-grpc` green: `GetInfo` returns the loaded DB's FOV range + pattern count; a `cedar_detect.proto`-shaped `ExtractCentroids` call succeeds unmodified (drop-in interop). — commit: "feat(ps-grpc): GetInfo, transport, and cedar-detect interop"

### Phase G — Finalize

- [ ] F1 — Workspace integration & status — deps: RPC4, DG6 — Run `cargo build --workspace` and `cargo test --workspace` (all crates incl. every parity test green); run `cargo clippy --workspace` and `cargo fmt --check` (fix or log); author `openspec/IMPLEMENTATION-STATUS.md` (feature→crate map, parity result per feature incl. any deferred items, how to run the server + the full test suite); add a "Build & Run" section to root `README.md`. — AC: `cargo build --workspace` and `cargo test --workspace` exit 0; `openspec/IMPLEMENTATION-STATUS.md` lists all six features with their parity outcomes; root `README.md` has a Build & Run section. — commit: "feat: workspace integration, parity summary, and implementation status"

## Decisions Log

- 2026-06-10 — Scope = implement `feat-01`…`feat-06` only (`ps-core`, `ps-detect`, `ps-db`, `ps-dbgen`, `ps-solve`, `ps-grpc`); `feat-07` mobile-runtime is OUT of this loop — user chose "stop at gRPC". The memory-mapped/linear-probe DB path still ships inside `ps-db` (PD5).
- 2026-06-10 — Token strategy = mechanical Rust coding delegated to a local Qwen subagent `ps-coder` (model `qwen3.6-27b` via the LiteLLM router at `localhost:4000`); architecture, task briefs, parity-fixture design, and **every** review on Opus via `ps-judge` (model `claude-opus-4-8`). User selected "judge every task" (max safety over token thrift). Subagent definitions live in `.claude/agents/ps-coder.md` and `.claude/agents/ps-judge.md`.
- 2026-06-10 — Parity ACs draw golden values from the **actual reference** (Phase-0 `tools/parity` Python venv + reference cedar-detect); user chose to install numpy/scipy etc. so the reference runs. Captured fixtures are committed under each crate's `tests/fixtures/`; never hand-fabricated.
- 2026-06-10 — `ps-dbgen` full count-parity vs `default_database.npz` is **deferred/best-effort**: no source star catalog (BSC5/HIP/TYC) ships in-repo, so DG6's reliable gate is determinism (byte-identical rebuild) + round-trip through `ps-db` + structural validity; count-parity is only asserted if the matching source catalog becomes available.
- 2026-06-10 — Toolchain ownership: the **user installs Rust/cargo + protoc**; the loop (S1) verifies and BLOCKs if missing rather than installing them. The Python reference env (S3) is the one thing the loop sets up, because parity capture needs it.
- 2026-06-10 — LiteLLM router (`localhost:4000`) accepts exactly `qwen3.6-27b`, `claude-opus-4-8`, `claude-sonnet-4-6` — there is **no `haiku`**, which is why default subagents 400; every subagent must pin one of the three. Custom `.claude/agents/*` register only at Claude Code **startup**, so the grind session must be (re)started for `ps-coder`/`ps-judge` to exist — S1 verifies this. Coding fallback if Qwen tool-use is unreliable: `claude-sonnet-4-6` (logged), not silent Opus.
- 2026-06-10 — Branch `feat/rust-implementation` off `docs/openspec-plate-solver` (the specs live there); commits are local-only (no push); the completed documentation loop was preserved by renaming its plan to `plan-openspec-docs.md`.

## Blocked Log

- (append-only) <task ID> — <reason> — <recommended fix>

## Run Log

- 2026-06-10 — plan authored — 38 tasks across 8 phases (Phase 0 + feat-01…feat-06 + finalize); subagents `ps-coder`(Qwen)/`ps-judge`(Opus) authored; docs plan archived to `plan-openspec-docs.md`. Loop starts at S1. NOTE: restart Claude Code before running `grind` so the custom agents register.
